(()=>{const el=document.querySelector('.academic-journey');if(!el||matchMedia('(prefers-reduced-motion: reduce)').matches)return;el.classList.add('is-ready');const start=()=>el.classList.add('is-running');if('IntersectionObserver'in window){const observer=new IntersectionObserver(entries=>{if(entries.some(e=>e.isIntersecting)){start();observer.disconnect()}},{threshold:.25});observer.observe(el)}else start()})();